// Add this to globals.css @layer components

// Reusable .input class for all form inputs
// Add to globals.css:
/*
@layer components {
  .input {
    @apply w-full px-3 py-2 text-sm border border-border rounded-lg bg-background
           focus:outline-none focus:ring-2 focus:ring-[hsl(var(--brand))]/30 focus:border-[hsl(var(--brand))]
           placeholder:text-muted-foreground/50 transition-colors;
  }
}
*/

export {}
